# Hugging Face Space runtime (ZeroGPU). gradio + spaces are preinstalled and
# platform-managed; torch is preinstalled — do not pin any of them here.
transformers>=4.49
accelerate>=1.0
torchvision
pillow>=10.0
pillow-heif>=0.18
av>=12.0
kokoro>=0.9
soundfile>=0.12
